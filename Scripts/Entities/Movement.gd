class_name Movement
extends Object


func move(
	_input_dir: Vector3,
	_prev_velocity: Vector3,
	_ground_friction: float,
	_ground_modifier: float,
	_is_grounded: bool,
	_delta: float
) -> Vector3:
	assert(false, "Unimplemented method.")
	return Vector3.ZERO
